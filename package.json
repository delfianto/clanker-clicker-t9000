{
  "name": "clanker-clicker-t9000",
  "version": "1.0.0",
  "private": true,
  "description": "Clean shortlink bypasser — MV3 browser extension",
  "type": "module",
  "scripts": {
    "build": "vp build",
    "build:firefox": "BROWSER=firefox vp build",
    "dev": "vp build --watch",
    "dev:firefox": "BROWSER=firefox vp build --watch",
    "check": "vp check && tsc -p tsconfig.json",
    "lint": "vp lint src/",
    "fmt": "vp fmt src/",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "debug": "bun run scripts/debug-bypass.ts"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.10.6",
    "@types/chrome": "^0.2.2",
    "@types/webextension-polyfill": "^0.12.5",
    "happy-dom": "^20.10.6",
    "playwright": "^1.61.1",
    "typescript": "^7.0.2",
    "vite-plugin-web-extension": "^4.5.1",
    "vite-plus": "^0.3.0"
  },
  "trustedDependencies": [
    "spawn-sync"
  ]
}
